export interface IEquipmentStatCardStyles {
  container: React.CSSProperties;
}

export const equipmentStatCardStyles: IEquipmentStatCardStyles = {
  container: {
    padding: "5px 0",
    borderBottom: "1px solid #eee",
    fontSize: "13px",
    color: "#444",
    alignItems: "center",
    display: "flex",
    gap: "4px",
  },
};
